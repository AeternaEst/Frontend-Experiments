
const increaseLevelAction = (id) => ({
    type: "INCREASE_LEVEL",
    id: id
})

const decreaseLevelAction = (id) => ({
    type: "DECREASE_LEVEL",
    id: id
})

export { increaseLevelAction, decreaseLevelAction };

